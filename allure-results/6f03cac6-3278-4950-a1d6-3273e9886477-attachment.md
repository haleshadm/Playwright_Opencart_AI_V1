# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: api\authentication.spec.ts >> Authentication API Tests >> POST - Successful Login @master @sanity @api
- Location: tests\api\authentication.spec.ts:16:9

# Error details

```
Error: Successful login should return HTTP 201

expect(received).toBe(expected) // Object.is equality

Expected: 201
Received: 401
```

# Test source

```ts
  1  | import { test, expect } from '@playwright/test';
  2  | import dotenv from 'dotenv';
  3  | import { Routes } from '../../api/endpoints/routes';
  4  | 
  5  | dotenv.config();
  6  | 
  7  | const BASE_URL = process.env.API_BASE_URL || Routes.BASE_URL;
  8  | const VALID_USERNAME = process.env.USERNAME || 'mor_2314';
  9  | const VALID_PASSWORD = process.env.PASSWORD || '83r5^_';
  10 | 
  11 | // ---------------------------------------------------------
  12 | // Authentication
  13 | // ---------------------------------------------------------
  14 | 
  15 | test.describe('Authentication API Tests', () => {
  16 |     test('POST - Successful Login @master @sanity @api', async ({ request }) => {
  17 |         const response = await request.post(`${BASE_URL}${Routes.AUTH_LOGIN}`, {
  18 |             data: { username: VALID_USERNAME, password: VALID_PASSWORD },
  19 |         });
  20 | 
> 21 |         expect(response.status(), 'Successful login should return HTTP 201').toBe(201);
     |                                                                              ^ Error: Successful login should return HTTP 201
  22 |         const body = await response.json();
  23 | 
  24 |         expect(body).toHaveProperty('token');
  25 |         expect(typeof body.token).toBe('string');
  26 |         expect(body.token.trim().length, 'The authentication token must not be empty').toBeGreaterThan(0);
  27 |     });
  28 | 
  29 |     test('POST - Invalid Login @master @regression @api', async ({ request }) => {
  30 |         const response = await request.post(`${BASE_URL}${Routes.AUTH_LOGIN}`, {
  31 |             data: { username: 'invalid-user', password: 'invalid-password' },
  32 |         });
  33 | 
  34 |         expect(response.status(), 'Invalid login should return HTTP 401').toBe(401);
  35 |         const body = await response.text();
  36 |         expect(body.trim()).toBe('username or password is incorrect');
  37 |     });
  38 | });
  39 | 
```